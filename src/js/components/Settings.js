import React from "react";
import AuthenticatedRoute from "./AuthenticatedRoute";

class Settings extends AuthenticatedRoute {
  render() {
    return (
      <div/>
    )
  }
}

export default Settings;
