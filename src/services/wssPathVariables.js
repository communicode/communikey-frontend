/**
 * The path variable for an access token.
 *
 * @constant
 * @default
 * @type {string}
 * @since 0.15.0
 */
export const ACCESS_TOKEN = "${accessToken}";

/**
 * The path variable for a job token.
 *
 * @constant
 * @default
 * @type {string}
 * @since 0.15.0
 */
export const TOKEN = "/${token}";